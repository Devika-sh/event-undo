<svg width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
<!-- Untitled UI `arrow-up-right`, built to the icon set's universal rules:
     stroke only, 2px, round cap + round join, glyph inset 2.25 in an 18x18 frame
     (the same proportional inset as calendar / clock / marker-pin / rupee).
     Painted as a CSS mask, so this stroke colour is only the mask alpha —
     the visible colour comes from an icon/* token on .btn-cta__icon. -->
<path id="Icon" d="M2.25 15.75L15.75 2.25M15.75 2.25H2.25M15.75 2.25V15.75" stroke="#000000" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
